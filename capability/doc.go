// Package capability holds the capability layer: resolver, adapter runtime,
// normalizers, identity resolver. Pure I/O + normalization — no reasoning,
// no x-interpretation production, derivation_mode always DIRECT.
// Lands in Phase B. See design/03-capability-layer.md.
package capability
