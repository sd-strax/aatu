# Built binaries
/bin/
/reckon
/reckon-backend
/reckon-adapter-*
/cmd/reckon/reckon
/cmd/reckon-backend/reckon-backend

# Go workspace files (local cross-repo dev only; see CLAUDE.md "Working across both repos")
go.work
go.work.sum

# Distribution outputs
dist/
vendor/

# Eval-run artifacts (transcripts + reports are local; baselines are committed)
eval/artifacts/

# Coverage and profiling
*.out
coverage.html

# IDE
.idea/
.vscode/
# ...except the workbench's, which is shipped project config: launch/tasks are
# how a contributor runs the extension, not personal editor state.
!workbench/.vscode/
*.swp
*~

# OS
.DS_Store
