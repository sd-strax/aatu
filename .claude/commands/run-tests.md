---
description: Run the Go test suite for this repo
---

Run `go test ./...` from the repo root. Report pass/fail counts and surface the names + error excerpts of any failing tests. On a clean pass, summarize the package count and test count. Don't run `go vet` or linters unless explicitly asked — keep this command focused.
